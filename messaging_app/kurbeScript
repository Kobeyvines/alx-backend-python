#!/usr/bin/env bash
# kurbeScript - Setup and check a local Kubernetes cluster with Minikube

# Exit on error
set -e

echo "🚀 Starting Kubernetes cluster with Minikube..."
minikube start

echo "✅ Verifying cluster info..."
kubectl cluster-info

echo "📋 Listing all pods in all namespaces..."
kubectl get pods --all-namespaces
